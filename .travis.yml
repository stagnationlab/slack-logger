sudo: false
language: node_js
node_js:
  - "10"
cache:
  yarn: true
  directories:
    - node_modules
branches:
  only:
    - master
script:
  - npm install
  - npm build
  - npm run lint
  - npm run prettier-check
#   - yarn coverage
# after_success:
#   - yarn coveralls
